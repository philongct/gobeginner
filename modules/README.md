# Learn Golang >=1.11 Modules
Folder structure:
- **remote**: A module which have versions on github
- **local**: A module intended to use for local only
- **main**: Main module which use `remote` and `local`
